import React, {useEffect, useState} from "react";
import {Route, Navigate} from "react-router-dom";

const AuthRoute = () => {

    let isAthorized = sessionStorage.getItem("user");

    return(
        <div>
        </div>
    )
}